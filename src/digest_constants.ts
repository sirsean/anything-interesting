/** Shared gates for digest + `/topnews` status badges (keep in sync with `digest.ts` rules). */
export const DIGEST_SOURCE_WINDOW_HOURS = 12;
export const MIN_DISTINCT_SOURCES = 3;
export const MIN_FINAL_SCORE = 0.6;
export const EXCEPTIONAL_SCORE = 0.88;
